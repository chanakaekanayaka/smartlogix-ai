# data/

Put the project's raw material here:

- **Dataset** – the logistics CSV/Excel files the system analyses.
- **Knowledge base** – text/markdown documents (shipping rules, FAQs,
  policies) that get loaded into ChromaDB by the script in `database/`.

This folder is tracked by Git, but large or private data files may be
ignored. Keep sample data small.
