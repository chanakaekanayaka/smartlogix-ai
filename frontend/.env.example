# Copy this file to ".env.local" to override the backend URL.
# The app already defaults to http://localhost:8000, so this is optional.
# Vite only exposes variables prefixed with VITE_.

VITE_API_BASE_URL=http://localhost:8000
